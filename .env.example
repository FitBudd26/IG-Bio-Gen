# Optional — HubSpot lead capture. App works fully without these.
# Both values are non-secret (safe for a client-side embed).
# Find them in HubSpot: Marketing → Forms → (your form) → Share/Embed.
VITE_HUBSPOT_PORTAL_ID=
VITE_HUBSPOT_FORM_GUID=
